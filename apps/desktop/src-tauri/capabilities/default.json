{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the EnvoyCoder window may ask the shell to do.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-pick-folder", "allow-daemon-shell"]
}
